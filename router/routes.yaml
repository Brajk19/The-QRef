Login:
    controller: Login
    action: showLogin

Logout:
    controller: Login
    action: logoutUser

VerifyLogin:
    controller: Login
    action: verifyLogin

Register:
    controller: Register
    action: showRegister

VerifyRegistration:
    controller: Register
    action: verifyRegistration

SuccessfulRegistration:
    controller: Login
    action: successfulRegistration

MainPage:
    controller: MainPage
    action: showMainPage

Settings:
    controller: Settings
    action: showSettings

ChangePasswordForm:
    controller: Settings
    action: changePasswordForm

VerifyNewPassword:
    controller: Settings
    action: verifyNewPassword

CreateQuiz:
    controller: CreateQuiz
    action: showCreateQuizPage

VerifyQuiz:
    controller: CreateQuiz
    action: verifyQuiz

AddRandomQuestion:
    controller: CreateQuiz
    action: addRandomQuestion

SolveQuizAnonymous:
    controller: SolveQuiz
    action: anonymous
    id: <id>

SolveQuizUser:
    controller: SolveQuiz
    action: loggedIn
    id: <id>

VerifyAnswers:
    controller: SolveQuiz
    action: verifyAnswers
    id: <id>        #this line is useless, have to include id because SolveQuiz controller
                    #requires id when creating new instance

AddComment:
    controller: Comments
    action: addComment

QuizList:
    controller: QuizList
    action: showAllQuizzes
    id: <id>

DetailedView:
    controller: QuizList
    action: detailedView
    id: <id>

EditQuestion:
    controller: EditQuiz
    action: editQuiz
    id: <id>

ApplyChanges:
    controller: EditQuiz
    action: applyChanges
    id: <id>

Stats:
    controller: Stats
    action: viewStats

Challenge:
    controller: Challenge
    action: chooseDifficulty

EasyChallenge:
    controller: Challenge
    action: easyChallenge

MediumChallenge:
    controller: Challenge
    action: mediumChallenge

HardChallenge:
    controller: Challenge
    action: hardChallenge

VerifyChallenge:
    controller: Challenge
    action: verifyChallenge

