Login:
    controller: Login
    action: showLogin

Logout:
    controller: Login
    action: logoutUser

VerifyLogin:
    controller: Login
    action: verifyLogin

Register:
    controller: Register
    action: showRegister

VerifyRegistration:
    controller: Register
    action: verifyRegistration

SuccessfulRegistration:
    controller: Login
    action: successfulRegistration

MainPage:
    controller: MainPage
    action: showMainPage

Settings:
    controller: Settings
    action: showSettings

ChangePasswordForm:
    controller: Settings
    action: changePasswordForm

VerifyNewPassword:
    controller: Settings
    action: verifyNewPassword

CreateQuiz:
    controller: CreateQuiz
    action: showCreateQuizPage

VerifyQuiz:
    controller: CreateQuiz
    action: verifyQuiz

AddRandomQuestion:
    controller: CreateQuiz
    action: addRandomQuestion

SolveQuizAnonymous:
    controller: SolveQuiz
    action: anonymous
    id: <id>

SolveQuizUser:
    controller: SolveQuiz
    action: loggedIn
    id: <id>

VerifyAnswers:
    controller: SolveQuiz
    action: verifyAnswers
    id: <id>        #this line is useless, have to include id because SolveQuiz controller
                    #requires id when creating new instance

QuizList:
    controller: UNKNOWN
    action: UNKNOWN

Stats:
    controller: UNKNOWN
    action: UNKNOWN

Challenge:
    controller: UNKNOWN
    action: UNKNOWN

